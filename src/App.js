import React from 'react';
import ReactDOM from 'react-dom';
import Root from './Root';

/* Don't change this render part!
You need to start to build your application in the Root.js! */
ReactDOM.render(
  <Root />,
  document.getElementById('root')
);